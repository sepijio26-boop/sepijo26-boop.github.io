# sepijo26-boop.github.io

GitHub Pages site (tic-tac-toe mini app in `index.html`).

## Clans plugin (Paper 1.21.4)

The [`clans-plugin/`](clans-plugin/) folder contains a complete clan plugin:

- `clans-plugin/README.md` — full documentation (English + a Persian quick guide)
- `clans-plugin/release/Clans-1.0.0.jar` — prebuilt plugin jar
- `clans-plugin/server/setup-server.sh` — one-command local Paper 1.21.4 test server
- `clans-plugin/src/` — source (Maven build: `mvn package`)
